#include "../include/logic/car.h"


// just for test.
void playerFunc0(Car* car, Map* map, Game* game)
{
    int frame = game->getTime();
    printf("%d\n", frame);
}

// just for test.
void playerFunc1(Car* car, Map* map, Game* game)
{
    int frame = game->getTime();
    printf("%d\n", frame);
}